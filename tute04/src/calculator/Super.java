package calculator;

public class Super {

    // Preconidtion that x must be 
    // between 0 and 100

    // as a post condition we expect
    // something between 
    // 0 and 100
    public int f(int x) {
        return x;
    }
}